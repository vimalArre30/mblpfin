// Intentionally empty — Turbopack panics on directories with .xml extension
// in the Next.js app router. Cache-Control fix is applied via next.config.ts
// headers() instead. See app/sitemap.ts for the active sitemap route.
export {};
